package com.example;
/**
 * Enumeration defining the possible states of a board intersection.
 * Used to identify player colors and empty spaces.
 */
public enum StoneColor {
    /** Represents the Black player's stone. */
    BLACK,
    /** Represents the White player's stone. */
    WHITE,
    /** Represents an empty intersection on the board. */
    EMPTY,
}
